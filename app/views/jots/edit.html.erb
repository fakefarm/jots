<h1><%= @jot.title %></h1>

<%= form_for(@jot) do |f| %>

  <div class="field">
    <%= f.text_area :body, placeholder: 'why?' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', jots_path %>

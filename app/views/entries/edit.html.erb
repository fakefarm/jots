<h1><%= @entry.title %></h1>

<%= form_for(@entry) do |f| %>

  <div class="field">
    <%= f.text_area :body, placeholder: 'why?' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', entries_path %>
